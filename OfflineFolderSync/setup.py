from setuptools import setup

setup(
    name="offlinefoldersync",
    version="0.1.0",
    packages=["offlinefoldersync"],
    author="Sofiya Debois",
    install_requires=[
        "PyQt5",
    ],
)
